--- a/src/cli/ui/types.ts
+++ b/src/cli/ui/types.ts
@@ -1,8 +1,15 @@
 /**
  * Type definitions for the CLI TUI components.
+ *
+ * MODEL LABEL DISPLAY - SINGLE SOURCE OF TRUTH:
+ * - App maintains `displayModel` state, derived from:
+ *   1. External `currentModel` prop (if provided by CLI)
+ *   2. Latest assistant message's `modelLabel` field
+ * - StatusBar displays `displayModel` as the active model indicator
+ * - ChatView displays each message's individual `modelLabel` (historical record)
+ * - These should always be consistent for the latest message
  */
 
 export type ViewMode = 'chat' | 'tools' | 'stats' | 'message';
 
@@ -11,9 +18,9 @@
   role: 'user' | 'assistant' | 'system';
   content: string;
   timestamp: string;
-  modelLabel?: string;
+  modelLabel?: string; // Model that generated this message (historical)
   toolCalls?: ToolCall[];
   stats?: MessageStats;
 }
